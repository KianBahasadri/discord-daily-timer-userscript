# Discord Daily Timer Userscript

A Tampermonkey userscript that replaces the Discord server title with:

- open time today
- focused time today
- tab switches today

File: `discord-server-title-daily-timer.user.js`
